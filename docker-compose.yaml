services:
  cdn:
    build: .
    ports:
      - "8080:8080"
      - "8443:8443"
      - "9090:9090"
    volumes:
      - ./config.yaml:/app/config/config.yaml:ro
      - ./public:/app/public:ro
      - ./certs:/app/certs:ro
